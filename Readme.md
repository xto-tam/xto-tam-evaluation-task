## EVALUATION-TASK

**The project consists of 3 docker containers:**

**2 containers -
React applications**

**1 сontainer - NestJS application**

**evaluation-client is available to http://localhost:3001**

**evaluation-admin for tracking client is available to http://localhost:3002**

**evaluation-server swagger is available to http://localhost:3000/api**


## How to setup project:

**BUILD & RUN:**
```docker-compose -f ./docker-compose.prod.yml up --build``` or ```npm run docker:prod``` in the directory of the projects


